# Codex Instructions

This is a Python Flask backend application with Jinja2 templates.
Use SQLAlchemy for database operations and Flask-Login for authentication.
Run with: `flask run --debug`
Test with: `python -m pytest`
